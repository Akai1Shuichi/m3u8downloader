<UserControl x:Class="m3u8Downloader.View.Controls.LabeledInput"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:m3u8Downloader.View.Controls"
             mc:Ignorable="d"
             x:Name="root">
    <Grid>
        <StackPanel>
            <!-- Label -->
            <TextBlock Text="{Binding Label, ElementName=root}" 
                   Foreground="White" 
                   Margin="0,0,0,4"/>
            <!-- Input -->
            <TextBox Text="{Binding Text, ElementName=root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 Padding="6"
                 BorderBrush="DodgerBlue"
                 BorderThickness="1"
                 Background="Black"
                 Foreground="White"
                 FontSize="14"/>
        </StackPanel>
    </Grid>
</UserControl>
